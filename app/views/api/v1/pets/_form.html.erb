<%= form_with(model: api_v1_pet) do |form| %>
  <% if api_v1_pet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(api_v1_pet.errors.count, "error") %> prohibited this api_v1_pet from being saved:</h2>

      <ul>
        <% api_v1_pet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
